import styled from 'styled-components';
import BaseButton from './button';

const CloseMonitorButton = styled(BaseButton)`
  position: absolute;
  right: 25%;
  left: 25%;
  margin: auto;
  background: #ed225d;
`

export default CloseMonitorButton;